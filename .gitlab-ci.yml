image: docker:latest
services:
  - docker:dind
before_script:
  - eval "$CI_BEFORE_SCRIPT"
after_script:
  - eval "$CI_AFTER_SCRIPT"


container-image:
  only:
    - master
  script:
    - docker build -t ${CI_REGISTRY_IMAGE}:latest .
    - docker push  ${CI_REGISTRY_IMAGE}:latest
