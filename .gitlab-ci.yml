image: docker:latest
services:
  - docker:dind
before_script:
  - eval "$CI_BEFORE_SCRIPT"
after_script:
  - eval "$CI_AFTER_SCRIPT"


container-image:
  only:
    - master
  script:
    - sh prepare.sh
    - apk add make
    - make build
    - make push
